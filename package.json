{
  "name": "@heliks/flex",
  "description": "Implementation of flexbox algorithm in typescript",
  "version": "0.0.2",
  "devDependencies": {
    "@types/jest": "29.0.1",
    "@types/node": "^20.14.2",
    "jest": "29.0.3",
    "ts-jest": "29.0.0",
    "typescript": "^4.9.5"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "clean": "rm -rf lib",
    "test": "jest --config jest.config.json",
    "deploy": "pnpm clean && pnpm build && pnpm publish",
    "deploy:dry": "pnpm clean && pnpm build && pnpm publish --dry-run"
  },
  "files": [
    "lib/*"
  ],
  "dependencies": {
    "-": "^0.0.1",
    "g": "^2.0.1",
    "pnpm": "^8.1.1"
  }
}
